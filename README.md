# Fraud-Detection
A multi-module fraud detection engine built for a hackathon, combining NLP, CNNs, and rule-based scanners into a unified Flask API and dashboard. Every scan returns a normalized 0–100 risk score across five action tiers, and the system runs entirely offline — no external API calls, no raw content stored.
